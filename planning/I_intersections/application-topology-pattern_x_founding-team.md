---
type: intersection
id: application-topology-pattern_x_founding-team
aspect: application-topology-pattern
audience: founding-team
dimension: 0
q_holes:
- application-topology-pattern—equity value and dilution
- application-topology-pattern—sweat equity recognition
- application-topology-pattern—product vision and execution
- application-topology-pattern—work-life sustainability
- application-topology-pattern—compensation and cash needs
- application-topology-pattern—role clarity and growth
- application-topology-pattern—mission alignment and impact
bridges:
- Add `equity value and dilution` as a synonym or linked facet on `application-topology-pattern` (matches audience concern).
- Add `sweat equity recognition` as a synonym or linked facet on `application-topology-pattern` (matches audience concern).
- Add `product vision and execution` as a synonym or linked facet on `application-topology-pattern` (matches audience concern).
- Add `work-life sustainability` as a synonym or linked facet on `application-topology-pattern` (matches audience concern).
- Add `compensation and cash needs` as a synonym or linked facet on `application-topology-pattern` (matches audience concern).
- Add `role clarity and growth` as a synonym or linked facet on `application-topology-pattern` (matches audience concern).
- Add `mission alignment and impact` as a synonym or linked facet on `application-topology-pattern` (matches audience concern).
message_invariants:
- Maintain consistent terminology aligned with audience vocabulary
- All claims must reference supporting evidence
risk_flags:
- Verify all q-holes are addressed before finalizing deliverable
---

# Intersection Summary

This intersection analyzes how **application-topology-pattern** relates to the **founding-team** audience. 
No direct hooks were found; consider adding bridges to strengthen the connection.
 There are 7 gap(s) (q-holes) that may need attention.

## Narrative Path (Traffic)

1. **Open with hooks**: Lead with the connection points that resonate with this audience's concerns.
2. **Address gaps**: Proactively address any q-holes by providing context or evidence.
3. **Close with action**: End with a clear call to action appropriate for this audience.

## Checklist

- [ ] Review all hooks for accuracy and relevance
- [ ] Address each q-hole with evidence or explanation
- [ ] Verify terminology matches audience vocab_map
- [ ] Check message invariants are maintained
- [ ] Validate against style guide